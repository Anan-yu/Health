services:
  mysql:
    ports: []
  redis:
    ports: []
  minio:
    ports: []
  rayk-ai:
    ports: []
  rayk-server:
    environment:
      WECHAT_MOCK_ENABLED: "false"
      WECHAT_AUTO_BIND_USERNAME: ""
    ports: []
  nginx:
    ports:
      - "${NGINX_PORT:-80}:80"
    restart: always
