services:
  mysql:
    ports: []
  redis:
    ports: []
  minio:
    ports: []
  rayk-ai:
    ports: []
  rayk-server:
    ports: []
  nginx:
    ports:
      - "${NGINX_PORT:-80}:80"
    restart: always

