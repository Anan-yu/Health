# AGENTS.md

本文件适用于 `E:\health` 整个项目树。任何 Agent、开发者或自动化工具在修改项目之前都必须遵守。

## 1. 开始工作前

1. 项目根目录就是 `E:\health`，不得再创建嵌套项目根目录。
2. 先阅读本文件、`handoff.md` 和 `README.md`。
3. 先执行 `git status --short`，识别并保护用户或其他 Agent 的未提交修改。
4. 只修改当前任务所需文件，不顺手重写无关模块。
5. 不得声称功能已完成，除非代码、构建或相应验证确实完成。

## 2. 永久架构约束

- Java 负责业务、数据、权限、流程编排和第三方业务服务接入。
- Python 负责 OCR、AI 评估、RAG、报告智能生成和随访智能处理。
- 客户端只有一个 UniApp 小程序，同时承载业务端和客户功能，并提供 H5 调试构建。
- 不创建独立 Vue 管理后台。
- MySQL、Redis、MinIO、Java、Python、Nginx 全部 Docker 化。
- Docker 镜像磁盘、容器磁盘、持久化数据和项目文件应放在 E 盘。
- 当前只允许三个角色：`PLATFORM_ADMIN`、`DOCTOR`、`CUSTOMER`。
- 不得重新引入机构管理员、健康管理师或其他已取消角色。
- 用户可见品牌统一使用“致宇健康”；旧 `rayk`/`RayK` 标识只可作为内部兼容标识，未经专项迁移不得随意修改数据库、包名和容器名。

## 3. 角色与数据范围

- 平台管理员：管理合作医院和医生预录入；查看全平台报告、原报告、PDF 和健康随访；回复平台反馈。
- 医生：当前试运行阶段对全平台体检者只读，可按姓名或手机号筛选，并可切换到个人客户工作台。
- 普通客户：只能查看和操作本人数据，不显示身份切换入口。
- 平台管理员不显示身份切换入口。
- 当前医生全平台只读是明确但临时的业务决定，任何 Agent 不得自行改成医院隔离或扩大为写权限。
- 所有权限必须由 Java 后端校验。前端隐藏页面、卡片或按钮不能作为唯一授权机制。

## 4. OCR 绝对规则

### 4.1 PDF 与图片必须独立

- PDF 和图片 OCR 必须使用独立路径。
- 修改图片识别、双栏切分、Qwen 提示词或图片质量策略时，不得改变 PDF 原生解析路径。
- 修改 PDF 解析时必须验证图片路径未回归。
- 禁止为“统一代码”而把两条路径强行合并。

### 4.2 原报告完整性

- 体检结果必须保持原顺序、原分类、原内容。
- 分类名称以原报告为准，不得自行起名、重分类或移动项目。
- 必须同时保留数值结果、非数值结果、参考值、单位、异常标识和检查小结。
- 姓名、年龄、电话、医院、科室、门诊号、住院号、床位号、设备号、条码、打印日期等元数据不得进入体检结果。
- 质量策略不得因过严而丢弃大量可用项目或把可识别报告判为失败。
- 不得将未经结构化、去噪和校验的原始 OCR 文本直接作为唯一事实交给大模型。

### 4.3 修改与验证

- OCR 改动必须使用真实 PDF、扫描 PDF、单栏图片和双栏图片做针对性回归。
- PDF 当前完整解析能力是受保护基线；任何图片优化都必须证明 PDF 结果未减少。
- 新增清洗规则时必须避免误删合法检查名称、文字结论和类目小结。

## 5. 前端构建永久规则

修改 `rayk-miniapp` 后，交付前必须同步生成并核对三个输出：

1. H5：`E:\health\rayk-miniapp\dist\build\h5`
2. 微信开发包：`E:\health\rayk-miniapp\dist\release\mp-weixin-dev`
3. 微信生产局域网包：`E:\health\rayk-miniapp\dist\release\mp-weixin-prod-lan`

必须先运行与改动相匹配的检查，通常包括：

```powershell
Set-Location E:\health\rayk-miniapp
npm run type-check
npm run lint
npm run build:h5
npm run build:mp-weixin:dev
npm run build:mp-weixin
```

- 两个 release 目录必须来自当次构建，不得保留旧包冒充已更新。
- H5 构建完成后确认 Nginx 实际挂载的是 `dist\build\h5`。
- `mp-weixin-prod-lan` 仅用于局域网验收，不等同于正式微信商店生产包。
- 真机局域网地址不得使用 `127.0.0.1`；正式发布必须使用 HTTPS 合法域名。

## 6. 数据、密钥与隐私

- 密钥只能从 `.env` 或部署平台密钥管理读取，不能写入源码、测试、文档、日志或 Git。
- `.env` 不得提交，也不要为了排障把其内容打印到终端或回复中。
- 对话或截图中暴露过的密钥必须在正式上线前轮换。
- MinIO 原报告和健康报告保持私有，通过鉴权接口或短时预签名地址访问。
- 健康数据属于敏感个人信息；查询必须遵守角色数据范围，不得通过前端参数绕过。
- 日志不得记录原始密钥、完整手机号、完整身份证号、微信会话密钥或完整健康报告内容。

## 7. 数据库与 Docker 安全

- Flyway 已执行迁移不得编辑或重排；数据库结构变化只能新增更高版本迁移。
- 数据库 `BIGINT` ID 到 JavaScript 必须按字符串传输和比较。
- 日常停止服务不得使用 `docker compose down -v`。
- 未经用户明确要求和备份确认，不得删除 MySQL、Redis、MinIO、日志或 OCR 模型数据卷。
- 不得用破坏性 Git 命令覆盖工作区，不得使用 `git reset --hard` 或擅自丢弃修改。
- 清理磁盘前必须核对绝对路径，不能把项目根目录、Docker 数据根或用户目录作为递归删除目标。

## 8. AI 与医疗边界

- DeepSeek、Qwen OCR、健康拍和腾讯云 TTS 的“代码已接入”与“生产已验收”必须分开表述。
- 所有外部调用必须有超时、错误处理、可观测信息和安全降级。
- AI 评估必须使用可追溯的结构化体检结果、健康档案、问卷和知识库证据。
- 不得把 AI 输出表述为确诊；可以提供可能疾病、风险线索和诊断参考，但必须保留健康管理与辅助参考边界。
- 不得为追求肯定语气而隐藏关键异常、伪造依据或杜撰医学知识来源。

## 9. 编辑与协作规则

- 不覆盖用户未提交的修改；发现重叠时先阅读差异并采用最小补丁。
- 本项目编辑源码和文档使用补丁式修改，避免无关全文件格式化。
- 不提交 `node_modules`、构建缓存、IDE 配置、本地微信配置、Docker 运行数据、模型缓存或日志。
- 不在未经用户请求时推送远端、创建 PR、删除数据或改变生产环境状态。
- 更新功能真实状态时同步维护 `handoff.md`；用户使用、架构或部署方式变化时更新 `README.md`。
- README 面向使用者，不记录逐次排障历史；handoff 记录当前状态，不堆积已废弃决策。

## 10. 完成定义

一次代码修改只有同时满足以下条件才可报告完成：

1. 目标行为已实现，未越权修改无关功能。
2. 相关自动化测试、类型检查、构建或最小可复现验证已通过。
3. OCR 改动同时核对 PDF 和图片路径。
4. 前端改动同步更新 H5、微信开发包和微信生产局域网包。
5. `git diff --check` 通过，差异中没有密钥、运行数据或意外大文件。
6. 用户已有修改被完整保留。
7. 最终说明明确区分已完成、未验证和仍需外部条件的事项。

