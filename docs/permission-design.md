# 权限与数据范围

前端的 `hasRole`、`hasPermission`、`usePermission` 和 `v-permission` 只控制展示。Spring Security 是最终授权入口；数据查询还会执行租户与客户范围校验。

- 平台管理员：一期平台基础查看；跨租户操作必须审计。
- 机构管理员：当前租户全部业务数据。
- 医生：仅分配给本人的客户，可审核和发布。
- 健康管理师：仅职责范围客户，可录入、确认和随访。
- 客户：`user_id` 关联的本人数据，且只看已发布健康报告。

`tenant_id` 来自 JWT/服务端上下文，不信任小程序参数。

